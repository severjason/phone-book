import styled from 'styled-components';

const ContactStyles = styled.div`
  margin: .5rem 0;
  
  .contact-header {
    width: 100%;
  }
  .card-action {
    width: 100%;
  }
`;

export default ContactStyles;
